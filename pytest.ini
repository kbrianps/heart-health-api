[pytest]
markers =
    unit: testes unitários isolados, sem subir o app nem tocar no banco real
    integration: testes de integração que sobem o app e usam banco em memória
testpaths = tests
addopts = -v --strict-markers
